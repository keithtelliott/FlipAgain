// import useLocalFlashcards from './useLocalFlashcards'

describe('useLocalFlashcards', () => {
  it('handles the happy path - it returns a custom hook', () => {
    // const [flashcards, setFlashcards] = useLocalFlashcards()
    console.log('GoDo:  How do I unit test a function that uses a hook?  ')
    expect('1').toBe('2')
  })
})
